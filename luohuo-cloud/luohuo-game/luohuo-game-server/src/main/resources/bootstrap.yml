server:
  port: 8086

spring:
  application:
    name: luohuo-game
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.yml

profiles:
  active: dev